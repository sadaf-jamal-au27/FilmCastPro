apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "filmcastpro.fullname" . }}-test-connection"
  labels:
    {{- include "filmcastpro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "filmcastpro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
